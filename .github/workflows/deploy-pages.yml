name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build library packages
        run: pnpm turbo run build --filter='./packages/*'

      - name: Build React playground
        run: pnpm turbo run build --filter=@intl-ui/playground
        env:
          GITHUB_PAGES: 'true'

      - name: Build Angular playground
        run: pnpm --filter @intl-ui/playground-angular build:ghpages

      - name: Assemble site
        run: |
          mkdir -p _site
          # Landing page at root
          cp apps/landing/index.html apps/landing/styles.css _site/
          # React playground at /react/
          mkdir -p _site/react
          cp -r apps/playground/dist/* _site/react/
          # Angular playground at /angular/
          mkdir -p _site/angular
          cp -r apps/playground-angular/dist/browser/* _site/angular/
          ls -la _site

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
