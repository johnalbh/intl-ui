{
  "name": "@intl-ui/landing",
  "version": "0.0.0",
  "private": true,
  "description": "Static landing page for johnalbh.github.io/intl-ui/",
  "scripts": {
    "dev": "npx -y serve -p 5180 .",
    "build": "mkdir -p dist && cp index.html styles.css dist/",
    "clean": "rm -rf dist .turbo"
  }
}
